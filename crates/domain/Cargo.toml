[package]
name = "visual_library_domain"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pure domain model for Visual Library (no I/O)"

[dependencies]
thiserror = { workspace = true }

[lib]
name = "visual_library_domain"
path = "src/lib.rs"
